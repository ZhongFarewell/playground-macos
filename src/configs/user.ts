import type { UserData } from "~/types";

const user: UserData = {
  name: "ZhongFarwell",
  avatar: "img/ui/avatar.png",
  password: "",
  // 登录用户名：对应后端 Users.username，登录页只输密码、用户名从这里读取
  username: "Aling"
};

export default user;
